# Tasks: add-mcp-gw-cli

## Tasks

- [x] Create `src/mcp_gateway/cli.py` with list, info, call, grep commands
- [x] Add `mcp-gw` entry point to `pyproject.toml`
- [x] Update `flake.nix` if needed (verify mcp-gw binary is available)
- [x] Add basic tests for CLI argument parsing
- [x] Update CLAUDE.md with mcp-gw in project overview and dev commands
